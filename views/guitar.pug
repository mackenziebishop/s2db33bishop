extends layout

block content
  h1= title 
  //- - var results = ["Acoustic", "Full", "150"];
  //- div(class="container-fluid")
  //-   div(class="row")
  //-   -
  //-   each item in results
  //-     div(col-sm-2) #{item}

  -let items = results
  div(class="container-fluid") 
    div(class="row m-1") 
      each item in items  
        - let destination = `dispatch(\"/guitar/update?id=${item._id}\")` 
        div(class="row-sm-2", class="m-1") Item #{item.guitar_type}(#{item.size}) is #{item.price} 
          button.btn( class="btn-primary ml-1", onclick= destination) Update   
  script.
   function dispatch(path){
     window.location.href=path
   }